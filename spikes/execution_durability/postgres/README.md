# PostgreSQL durability spike

The PostgreSQL side includes compatible schema and transaction prototypes. Runtime scenarios execute only when safe local PostgreSQL tooling and a Python driver are already available without dependency installation, external credentials, or service changes. In the captured environment those capabilities were unavailable, so runtime scenarios are marked `NOT_EXECUTED_ENVIRONMENT_UNAVAILABLE`.
